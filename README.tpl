# {{crate}}

{{readme}}

## Usage

Add this crate as a dependency in your `Cargo.toml`:

```toml
[dependencies]
embedded-io = "0.7.1"
embedded-io-async = "0.7.0"

[dev-dependencies]
mock-serial-async = "{{version}}"
```

# License

{{license}}